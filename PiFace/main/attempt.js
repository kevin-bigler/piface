// TODO not really sure I need this -- may just delete later
pf.Attempt = function() {
  this.row = null;
  this.definition = null;
  this.approach = null;

  /*
    row
    definition
    approach

    compute()
    getActions()
    computeActions()
  */
};

// pf.Attempt.prototype.property = value;
// pf.Attempt.prototype.func = function() {};

pf.Attempt.prototype.computeActions = function() {
  // TODO
};
